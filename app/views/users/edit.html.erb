<h1>ユーザー情報の編集</h1>

<%= form_for(@user) do |f| %>

  <%= f.label :name, 'Name' %>
  <%= f.text_field :name %>
  
  <%= f.label :email, 'Email' %>
  <%= f.email_field :email %>
  
  <%= f.submit '保存' %>
  
<% end %>